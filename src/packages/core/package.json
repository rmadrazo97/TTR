{
  "name": "@ttr/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared TTR POC core: config, Postgres access, repos, storage, email, and the extraction interface. The single dependency for all apps.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8",
    "@aws-sdk/client-s3": "^3",
    "@aws-sdk/s3-request-presigner": "^3",
    "nodemailer": "^6",
    "dotenv": "^16"
  },
  "devDependencies": {
    "@types/pg": "^8",
    "@types/nodemailer": "^6"
  }
}
